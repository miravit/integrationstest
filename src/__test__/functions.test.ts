/* 

describe("movieSort", () => {
  test("vet inte", () => {
    //arrange
    let movies: IMovie[] = [
      {
        Title: "b",
        imdbID: "0700818",
        Type: "hej",
        Poster: "posterul",
        Year: "1998",
      },
      {
        Title: "a",
        imdbID: "0700818",
        Type: "hej",
        Poster: "posterul",
        Year: "1998",
      },
    ];
    let desc: boolean = true;

    let myFunction: IMovie[] = functions.movieSort(movies, desc);

    expect(myFunction).toHaveReturnedWith(-1);
  });
   test("vet inte", () => {
        //arrange
        let movies:IMovie[] = []
    })
}); 
 */
